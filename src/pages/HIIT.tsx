interface HIITProps {}

const HIIT: React.FC<HIITProps> = ({}) => {
  return (
    <div className="flex flex-col w-full h-full justify-center items-center">
      HIIT
    </div>
  )
}

export default HIIT
